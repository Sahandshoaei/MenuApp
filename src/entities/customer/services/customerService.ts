import type { Customer } from "../types/customer";

export const customerService = {

    register(customer: Customer) {

        return customer;

    },

    update(customer: Customer) {

        return customer;

    },

};

// فعلاً فقط Fake Service هست.

// بعداً API میشه.